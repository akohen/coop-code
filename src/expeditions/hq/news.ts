import { Node } from "../../typings";
import { em } from "../../utils";

export const news:Node['files'] = {
  news: `${em('2020-03-21')}
I'm starting to expose the currently deployed version (visible with the ${em('stats')} command), as well as keeping news here.
The first items will probably not be archived, though this should come eventually.
Expedition2 is playable, but lacks polish`
}