const nodes = ['test','room 1 description', 'puzzle 1 question, input 42 to win'];
const players = [{name: 'Bob', location:0}]
const answers = [42]

const expedition = {nodes, players}

export default expedition;
